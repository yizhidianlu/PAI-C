"""Library helpers — retrieval, clustering, claims, revisions.

Distinct from ``paic.mcp_server.tools.library`` (the MCP tool wrappers for
``s2_search`` / ``dedupe`` / ``library_add``). This package is the home for
section-aware retrieval (BM25 + MMR), related-work clustering, the claim
ledger, and revision queue helpers introduced by the paper-quality roadmap.
"""
